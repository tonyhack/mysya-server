package protocol;

message CombatRoleFields {
  optional uint64 id = 1;                             // 角色id
  optional string name = 2;                           // 角色名称
  optional int32 camp_id = 3;                         // 阵营id
}
